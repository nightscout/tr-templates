

box: bewest/jekyll-pandoc-plugin-box@0.0.1

build:
  steps:
      - script:
          name: "make install and build"
          code: |-
            export
            whoami
            ls -alh $HOME
            sudo gem install bundle
            make install
            make build
            make travis-upload
