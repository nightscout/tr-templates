

box: bewest/jekyll-pandoc-plugin-box@0.0.2

build:
  steps:
      - script:
          name: "make install and build"
          code: |-
            export
            whoami
            ls -alh $HOME
            make install
            make build
            make travis-upload
