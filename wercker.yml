

box: bewest/jekyll-pandoc-plugin-box@0.0.3

build:
  steps:
      - script:
          name: "make install and build"
          code: |-
            ls -alh $HOME
            make install
            make wercker-build

deploy:
  steps:
      - script:
          name: "push to gh-pages"
          code: |-
            make wercker-deploy
